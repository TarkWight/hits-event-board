# Порт, на котором будет слушать backend (по умолчанию 8080)
PORT=8080

# Подключение к базе Postgres
# Если используешь локальную роль без пароля (peer auth):
# DATABASE_URL=postgres://<username>@localhost:5432/<db_name>
# Если с паролем:
# DATABASE_URL=postgres://<username>:<password>@localhost:5432/<db_name>
DATABASE_URL=

# Уровень логирования (info, debug, warn, error)
RUST_LOG=info

# OAuth Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_REDIRECT_URL=http://localhost:8080/oauth/google/callback